@model TheCatProject.Models.Trait


<div class="row">
    <div class="col-md-12">
        <img src="~/Content/Images/cat6.png" class="img-fluid" width="250" height="188" />
    </div>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    @* This needs to go - no dropdown, just display name (set value)
        FIX ME LATER ------------------------------------*@
    @*
    <div class="row">
        <div class="col-md-12">
            @Html.DropDownList("CatID", null, htmlAttributes: new { @class = "boxes" })
        </div>
    </div> *@
  
    <div class="row">
        <div class="col-md-12">
            <h4 id="pageheaders">Color:</h4>
            @Html.DropDownList("ColorID", null, htmlAttributes: new { @class = "boxes" })
            @Html.ValidationMessageFor(model => model.ColorID, "", new { @class = "text-danger" })
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <h4 id="pageheaders">Breed:</h4>
            @Html.DropDownList("BreedID", null, htmlAttributes: new { @class = "boxes" })
            @Html.ValidationMessageFor(model => model.BreedID, "", new { @class = "text-danger" })
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-md-12">
            <input type="submit" value="add cat traits" class="myButton" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
